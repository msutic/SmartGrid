import { MultimediaWorkplan } from './multimedia-workplan';

describe('MultimediaWorkplan', () => {
  it('should create an instance', () => {
    expect(new MultimediaWorkplan()).toBeTruthy();
  });
});
