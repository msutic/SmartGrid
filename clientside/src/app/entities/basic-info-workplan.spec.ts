import { BasicInfoWorkplan } from './basic-info-workplan';

describe('BasicInfoWorkplan', () => {
  it('should create an instance', () => {
    expect(new BasicInfoWorkplan()).toBeTruthy();
  });
});
