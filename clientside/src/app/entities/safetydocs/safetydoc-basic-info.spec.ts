import { SafetydocBasicInfo } from './safetydoc-basic-info';

describe('SafetydocBasicInfo', () => {
  it('should create an instance', () => {
    expect(new SafetydocBasicInfo()).toBeTruthy();
  });
});
